import React from "react";
import styled from "styled-components";

const LoginContainer = styled.div``;
const LoginLink = styled.a``;

const Login = () => {
  return (
    <LoginContainer>
      <LoginLink>Login with Spotify</LoginLink>
    </LoginContainer>
  );
};

export default Login;
